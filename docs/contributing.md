# Contribuicao

## Padroes

- Mantenha endpoints organizados por dominio em `projects/*/api`.
- Atualize a documentacao em `docs/` ao alterar comportamento.
- Prefira configuracoes em `shared/infrastructure/config` e `projects/*/config`.
