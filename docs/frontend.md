# Frontend

## Scripts

- `npm run dev` (porta 3001)
- `npm run build`
- `npm run start`

## Variaveis de ambiente

- `NEXT_PUBLIC_API_URL`
- `NEXT_PUBLIC_APP_NAME`
- `NEXT_PUBLIC_FACEBOOK_APP_ID`

## Docker

Imagem e build definidos em `frontend/Dockerfile` com deploy via `docker-compose.yml`.
